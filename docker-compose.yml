# Egypt Supermarket Management System - Local development
# Usage: docker-compose up -d
# API: http://localhost:3000  Health: http://localhost:3000/health

services:
  api:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
      PORT: 3000
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-in-production}
      CORS_ORIGIN: http://localhost:5173,http://localhost:3000
    # When DB/Redis are added, uncomment:
    # env_file: .env
    # depends_on:
    #   - postgres
    #   - redis

  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_USER: esms
  #     POSTGRES_PASSWORD: ${PG_PASSWORD:-esms}
  #     POSTGRES_DB: esms
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

  # redis:
  #   image: redis:7-alpine
  #   volumes:
  #     - redisdata:/data
  #   ports:
  #     - "6379:6379"

# volumes:
#   pgdata:
#   redisdata:
